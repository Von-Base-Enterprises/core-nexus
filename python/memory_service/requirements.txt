# Core Nexus Memory Service Dependencies
# BUILD_CACHE_BUST: 2025-06-28T22:45:00Z - Force fresh dependency installation

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data models and validation
pydantic==2.5.0

# Database drivers
asyncpg==0.29.0           # PostgreSQL async driver for pgvector
psycopg2-binary==2.9.9    # Fallback PostgreSQL driver

# Vector stores
chromadb==0.4.18          # Local vector storage
pinecone-client==3.0.0    # Cloud vector storage

# Vector database support
pgvector==0.2.5           # PostgreSQL vector extension support

# Machine learning / embeddings
numpy==1.24.3
openai==1.3.6             # For embeddings integration

# Async support
asyncio-pool==0.6.0
aiofiles==23.2.1              # Async file operations for task queue persistence
redis>=4.5.0                  # Redis client for caching layer

# Monitoring and logging
structlog==23.2.0
loguru==0.7.2

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0         # Test coverage reporting
pytest-mock==3.12.0       # Enhanced mocking capabilities
pytest-timeout==2.2.0     # Test timeout management
httpx==0.25.2             # For testing FastAPI endpoints
faker==19.13.0            # Test data generation

# Production utilities
gunicorn==21.2.0
prometheus-client==0.19.0  # For metrics export
prometheus-fastapi-instrumentator==6.1.0  # FastAPI Prometheus integration

# OpenTelemetry Core
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation==0.45b0

# OpenTelemetry Instrumentation
opentelemetry-instrumentation-fastapi==0.45b0
opentelemetry-instrumentation-asyncpg==0.45b0
opentelemetry-instrumentation-httpx==0.45b0
opentelemetry-instrumentation-logging==0.45b0

# OpenTelemetry Exporters
opentelemetry-exporter-otlp==1.24.0
opentelemetry-exporter-prometheus==0.45b0
opentelemetry-exporter-otlp-proto-grpc==1.24.0

# OpenTelemetry Utilities
opentelemetry-semantic-conventions==0.45b0
opentelemetry-propagator-b3==1.24.0

# Graph integration dependencies (for Knowledge Graph)
spacy>=3.5.0              # Entity extraction with NLP
# Note: spacy model needs to be downloaded separately:
# python -m spacy download en_core_web_sm

# Data processing
pandas==2.0.3

# JWT Authentication & Security (CRITICAL: Required for auth system)
python-jose[cryptography]==3.3.0   # JWT token handling and validation
passlib[bcrypt]==1.7.4              # Secure password hashing with bcrypt
python-multipart==0.0.9             # FastAPI form data and file uploads
bcrypt==4.1.2                       # Cryptographic hashing backend

# Additional HTTP clients for integrations
aiohttp==3.9.1                      # Async HTTP client for external APIs

# Google AI/Gemini integration dependencies
google-generativeai==0.8.5          # Gemini AI SDK

# Resilience and retry logic
tenacity==8.2.3                     # Retry patterns and resilience

# Graph database support
neo4j==5.15.0                       # Neo4j graph database driver

# Machine learning models for embeddings
sentence-transformers==2.2.2        # Pre-trained transformer models for embeddings

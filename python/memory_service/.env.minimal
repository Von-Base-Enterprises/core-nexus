# Core Nexus Memory Service - Step 1 Minimal Configuration
# This file contains ONLY the essential variables needed for Step 1 validation

# === REQUIRED DATABASE CONFIGURATION ===
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=core_nexus_minimal
POSTGRES_USER=core_nexus
POSTGRES_PASSWORD=minimal_test_password

# === BASIC SERVICE CONFIGURATION ===
SERVICE_NAME=core-nexus-memory-minimal
ENVIRONMENT=development
LOG_LEVEL=INFO

# === OPTIONAL PROVIDERS (DISABLED FOR STEP 1) ===
# We'll enable these in later steps
PINECONE_API_KEY=""
OPENAI_API_KEY=mock_key_for_testing

# === PERFORMANCE (CONSERVATIVE FOR TESTING) ===
WORKERS=1
MAX_CONNECTIONS=100
QUERY_TIMEOUT_MS=10000

# === STEP 1 VALIDATION FLAGS ===
ENABLE_METRICS=false
ENABLE_TRACING=false
ENABLE_EVOLUTION=false